// On crée une interface car on va juste utiliser les infos et aucune modif ne sera ajoutée
// On va l'utiliser que la connexion au dashboard

export interface User {
  email: string;
  firstName: string;
  lastName: string;
}
